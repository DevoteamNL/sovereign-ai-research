---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.clusterLogging.operatorGroup.name }}
  namespace: {{ .Values.clusterLogging.namespace.name }}
spec:
  upgradeStrategy: {{ .Values.clusterLogging.operatorGroup.upgradeStrategy }}
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.lokiOperator.operatorGroup.name }}
  namespace: {{ .Values.lokiOperator.namespace.name }}
spec:
  upgradeStrategy: {{ .Values.lokiOperator.operatorGroup.upgradeStrategy }}
