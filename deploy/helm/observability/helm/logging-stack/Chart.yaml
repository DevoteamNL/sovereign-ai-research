apiVersion: v2
name: logging-stack
description: >
  A helm chart to deploy LokiStack logging stack with MinIO storage for OpenShift (requires logging-operator to be installed first)
type: application
version: 0.1.0
appVersion: 0.1.0
